global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'telemetry_infrastructure'
    file_sd_configs:
      - files:
        - '/etc/prometheus/targets.json'
        refresh_interval: 15s